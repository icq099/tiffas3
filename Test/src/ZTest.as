package
{
	import flash.display.Sprite;
	
	import yzhkof.BasicAsProject;

	public class ZTest extends BasicAsProject
	{
		public function ZTest()
		{
			/* var t:TextField=new TextField();
			var f:TextFormat=new TextFormat();
			f.size=30;
			t.htmlText="<b>广东</b>";
			t.setTextFormat(f);
			t.filters=[new GlowFilter()];
			//addChild(t)
			addChild(new Bitmap(ToolBitmapData.getInstance().drawDisplayObject(t))); */
			var xml:XML=new XML();
			var a:Sprite=new Sprite();
			
		}
	}
}